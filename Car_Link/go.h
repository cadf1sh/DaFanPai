#ifndef GO_H
#define GO_H

#include "headfile.h"

extern uint8 close_Threshold;
extern uint8 far_Threshold;
extern uint8 mid_Threshold;
extern uint8 always_camera_flag;
extern uint8 always_camera_delay;
extern uint8 toptemp;

void go(void);
#endif // GO_H
